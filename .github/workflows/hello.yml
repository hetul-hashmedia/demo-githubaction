name: Hello Workflow

on:
  push:
    branches:
      - dev

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Print message
        run: echo "Hello from GitHub Actions 🚀"
