name: Hello Workflow

on:
  push:
    branches:
      - dev

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print message
        run: echo "Hello Hetul! GitHub Actions is working 🚀"
